name: Build, Lint, and Test

on:
  push:
    branches:
    - "*"
  pull_request:
    branches: [ main ]

jobs:
  build_and_test:
    uses: cscoding21/cscoding/.github/workflows/go-build.yaml@main
    
